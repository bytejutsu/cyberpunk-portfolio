import React from 'react';

const CyberNeonText = ({ text }) => (
    <h1 className="text-4xl font-mono text-pink-400 animate-pulse tracking-widest neon-glow">
        {text}
    </h1>
);

export default CyberNeonText;